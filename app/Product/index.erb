<div class="pageTitle">
  <h1>Products</h1>
</div>

<div class="toolbar">
  <!-- TODO: handle home case -->
  <!--<a class="button leftButton blueButton" href="#" onclick="window.location = '<%= Rho::RhoConfig.start_path %>'; return false;">Home</a>-->
  <a class="button" href="<%= url_for :action => :new %>">New</a>
</div>

<div class="content">
  <ul>
    <% @products.each do |product| %>
    
        <li>
          <a href="<%= url_for :action => :show, :id => product.object %>">
            <%= product.name %>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>